# Stage 10 — Motor output
#
# Final rate-coded layer driving action execution. Fast timescale (5ms)
# so motor commands can change on tight loops.

export population motor {
    count: 256,
    dynamics: "rate_code",
    equation: "y = ReLU(x)",
    timescale: 0.005,
    capacity: 1.0
}
